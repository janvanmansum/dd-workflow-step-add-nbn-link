#
# dd-workflow-step-add-nbn-link
#

server:
  applicationContextPath: /
  adminContextPath: /
  applicationConnectors:
    - type: http
      # Replace with port number unique for this service
      port: 20000
  adminConnectors:
    - type: http
      # Replace with port number unique for this service
      port: 20001

#
# See https://www.dropwizard.io/en/latest/manual/configuration.html#logging
#
logging:
  level: INFO
  appenders:
    - type: file
      archive: true
      logFormat: "%-5p [%d{ISO8601}] [%t] %c: %m%n%rEx"
      currentLogFilename: /var/opt/dans.knaw.nl/log/dd-workflow-step-vault-metadata/dd-workflow-step-vault-metadata.log
      archivedLogFilenamePattern: /var/opt/dans.knaw.nl/log/dd-workflow-step-vault-metadata/dd-workflow-step-vault-metadata-%d.log
      archivedFileCount: 5
#
# Configures the threads on which the vault metadata setting actions are executed.
#
taskQueue:
  nameFormat: "task-queue-thread-%d"

  maxQueueSize: 4
  # Number of threads will be increased when maxQueueSize is exceeded.
  minThreads: 2
  # No more than maxThreads will be created though
  maxThreads: 10
  # Threads will die after 60 seconds of idleness
  keepAliveTime: 60 seconds

dataverse:
  baseUrl: 'http://localhost:8080/'
  apiKey: changeme
  awaitLockStateMaxNumberOfRetries: 5
  awaitLockStateMillisecondsBetweenRetries: 3000
